# Состояние на 12 сентября 2026

Версия: 0.1.0-dev.1. Начало разработки, не законченный аналог RustDesk.

Проверено локально:
- 16 Python unit/integration tests: PASS, unittest, 0.507 секунды.
- Загрузка строго libxdo.so.3; отрицательная проверка похожего libxdot.
- CLI JSON и коды выхода диагностики, изоляция загрузки нативной библиотеки.
- Реальное получение локального Git checkout по SHA, сохранность пользовательских
  файлов и отказ от перезаписи существующей директории.

Написано, но не исполнено:
- 17 Rust unit tests для policy crate (нет cargo/rustc в среде).
- GitHub Actions workflow (нет созданного удалённого репозитория/запуска CI).

Не завершено:
- Сборка/запуск форка RustDesk и реальный GUI-тест на Windows/Simply.
- Сетевая загрузка upstream через bootstrap с подмодулями: код есть, не проверена.
- Интеграция policy, токены, API, БД, панель, hbbs/hbbr, подписанные обновления.

GitHub-репозитории пользователя не создавались и не изменялись.
Имя ZeroDisk имеет точное совпадение. Новый предложенный пользователем кандидат
ZHeroDiZk проверен: точных совпадений не найдено; см. NAME-CHECK.md.
Пользователь пока попросил проверку кандидата, а не окончательно утвердил имя.
